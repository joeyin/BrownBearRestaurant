<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KFC</title>
    <link rel="icon" href="/img/favicon.ico">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css">
    <link rel="stylesheet" type="text/css" href="/css/app.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
        rel="stylesheet">
</head>

<body>
    <%- include('../partials/header') %>
    <main>
        <!-- Wrapper with Border -->
        <div class="container">
            <h1 class="header">Order Receipt</h1>
            <div class="flex items-center flex-col lg-flex-row rounded-2xl shadow-sm p-5">
                <!-- Left section (Image of the meal) -->
                <div class="flex-1">
                    <img width="100%" src="<%=order.product.imgsrc%>" alt="<%=order.product.name%>" class="meal-image">
                </div>

                <!-- Right section (Details and Price) -->
                <div class="flex-1">
                    <!-- <h2>Order Receipt</h2> -->
                    <p><strong>Order number:</strong>
                        <%=order.id%>
                    </p>
                    <p><strong>Product Name:</strong>
                        <%=order.product.name%>
                    </p>
                    <p><strong>Quantity:</strong> 1</p>
                    <p><strong>Total Amount:</strong> $<%=order.totalAmount%>
                    </p>
                    <p><strong>Status:</strong>
                        <%=order.status%>
                    </p>
                    <p><strong>Recipient Name:</strong>
                        <%=order.customer_name%>
                    </p>
                    <p><strong>Delivery Address:</strong>
                        <%=order.shippingAddress%>
                    </p>
                    <p><strong>Order Date:</strong>
                        <%= new Date(order.createdAt).toLocaleString('en-US', { weekday: 'short' , year: 'numeric' ,
                            month: 'short' , day: 'numeric' , hour: '2-digit' , minute: '2-digit' , hour12: true })
                            %>
                    </p>
                </div>
            </div>
        </div>
    </main>
    <%- include('../partials/footer') %>
</body>

</html>