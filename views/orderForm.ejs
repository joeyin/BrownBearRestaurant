<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KFC</title>
    <link rel="icon" href="/img/favicon.ico">
    <link rel="stylesheet" type="text/css" href="/css/orderform.css">
    <link rel="stylesheet" type="text/css" href="/css/header.css">
    <link rel="stylesheet" type="text/css" href="/css/footer.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap"
        rel="stylesheet">
</head>

<body>
    <%- include('partials/header') %>
    <div class="container">
        <!-- Left section (Image of the meal) -->
        <div class="image-section">
            <img src="<%=product.imgsrc%>" alt="Family Bucket Feast" class="meal-image">
        </div>

        <!-- Right section (Details and Price) -->
        <div class="info-section">
            <h1><%=product.name%></h1>
            <p class="price">$<%=product.price%></p>
            <p><%=product.describtion%></p>
            <form action="/orders" method="POST">
                <h1>Delivery Info:</h1>
                <div class="form-group">
                    <label for=" customer_name"> recipient name:</label>
                    <input type="text" id="customer_name" name="customer_name" required placeholder="Enter recipient name"
                        class="form-control">
                </div>
                <div class="form-group">
                    <label for=" delivery_addres"> delivery addres:</label>
                    <input type="text" id="delivery_addres" name="delivery_addres" required placeholder="Enter delivery addres"
                        class="form-control">
                </div>
                <input type="text" name="product_id" value="<%=product._id%>" style="display: none;">
                <input type="number" name="total_amount" value="<%=product.price%>" style="display: none;">
                <button class="order-btn">Place Order &nbsp;&nbsp; $<%=product.price%></button>
            </form>
        </div>
    </div>
    <%- include('partials/footer') %>
    </div>
</body>

</html>